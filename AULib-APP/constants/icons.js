import { Ionicons, FontAwesome } from "@expo/vector-icons";

export default {
  home: <Ionicons name="home" size={24} color="#FFF" />, // Home icon
  notifications: <Ionicons name="notifications" size={24} color="#FFF" />, // Notifications icon
  user: <FontAwesome name="user-circle" size={24} color="#FFF" />, // User icon
  globe: <Ionicons name="globe" size={24} color="#FFF" />, // Globe icon
  back: <Ionicons name="arrow-back" size={24} color="#FFF" />, // Back icon
  forward: <Ionicons name="arrow-forward" size={24} color="#FFF" />, // Forward icon
  refresh: <Ionicons name="refresh" size={24} color="#FFF" />, // Refresh icon
  book: <Ionicons name="book" size={24} color="#FFF" />, // Book icon
  search: <Ionicons name="search" size={24} color="#FFF" />, // Search icon
  arrowup: <Ionicons name="arrow-up" size={24} color="#FFF" />, // Arrow up icon
  close: <Ionicons name="close" size={24} color="white" />, // Close icon
};
